# dataclasses - Notes

## What is it?
`pdb` is a python library that implements a python debugger.
by dropping `import pdb; pdb.set_trace()` it stops the code at that exact spot and you can print anything with `p`.. other 1 letter commands also exist.