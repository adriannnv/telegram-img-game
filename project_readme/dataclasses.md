# dataclasses - Notes

## What is it?
`dataclasses` is a python library that implements class boilerplate for python code.
When adding `@dataclass` before the definition of a class it generates the `__init__` and `__eq__` implementations for you, alongside a few more. 
